#ifndef TYPES_H
#define TYPES_H

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int do_view(char *filename);
int convert_size(unsigned char *size);
int do_edit(char *filename);

#endif